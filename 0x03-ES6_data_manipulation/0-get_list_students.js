export default function getListStudents() {
  const newArray = [];
  newArray.push({ id: 1, firstName: 'Guillaume', location: 'San Francisco' });
  newArray.push({ id: 2, firstName: 'James', location: 'Columbia' });
  newArray.push({ id: 5, firstName: 'Serena', location: 'San Francisco' });
  return newArray;
}
